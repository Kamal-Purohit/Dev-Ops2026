provider "aws" {
  region = "ap-south-1"
}

terraform {
  required_version = ">=0.12"

  backend "s3" {
    bucket         = "terraform-demo-s3-bucket-devenv"
    key            = "dev/terraform.tfstate"
    region         = "ap-south-1"
      }
}